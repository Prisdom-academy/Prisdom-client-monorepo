import { ExtendedColor } from '../../colors/interfaces';

export const typoDef = {
  type_neutral_default: {
    default: ExtendedColor['darkLevel.400']
  },

  type_neutral_disable: {
    default: ExtendedColor['darkLevel.600']
  },

  type_link_focus: {
    default: ExtendedColor['info_dark.300']
  },

  type_placeholder: {
    default: ExtendedColor['darkLevel.600']
  },

  type_neutral_hard: {
    default: ExtendedColor['darkLevel.200']
  },

  type_neutral_medium: {
    default: ExtendedColor['darkLevel.300']
  },
  type_neutral_light: {
    default: ExtendedColor['darkLevel.500']
  }
};
