import { defineStyle } from "@chakra-ui/react";

export const styles = defineStyle({
  root: {
    minH: "3rem",
    minW: "16.25rem",
    alignItems: "center",
    px: ".75rem",
    cursor: "pointer",

    _hover: {
      bgColor: "rgba()",
    },
  },
});
